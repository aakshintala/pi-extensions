import { describe, it } from "node:test";
import assert from "node:assert";

describe("pi-extensions skeleton", () => {
  it("loads (runtime surface is audited in tests/audit.test.mjs)", () => {
    assert.ok(true);
  });
});
