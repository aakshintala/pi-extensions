import { describe, it } from "node:test";
import assert from "node:assert";

describe("pi-extensions skeleton", () => {
  it("loads (placeholder until audit harness lands)", () => {
    assert.ok(true);
  });
});
