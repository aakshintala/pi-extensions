# clear

`/clear`: starts a new session, as `/new` does. Spec:
[#36](https://github.com/aakshintala/pi-rig/issues/36).

## Tools, keys and settings

None.

## Upstream

Replaces the single-file `~/.pi/agent/extensions/clear.ts` (no licence, the
user's own).
